[project]
name = "my-lib"
version = "0.42.2"
description = ""
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
]

[dependency-groups]
dev = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["my_lib"]
